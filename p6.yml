---
# BASH shell clish command
 - name: gather information6
   connection: network_cli
   hosts: ckp
   tasks:

     - name: get config 
       cli_command:
         command: clish -c "show interface eth0"
       register: result1

     - name: DEBUG section
       debug: var=result1.stdout_lines
#       debug: var=result1
